[project]
name = "app-win"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    # pywin32 is a Windows-only COM/WinAPI binding.
    "pywin32>=306 ; sys_platform == 'win32'",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
