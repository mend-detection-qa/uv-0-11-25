[project]
name = "app-linux"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    # h2 is an HTTP/2 transport — only needed on Linux in this probe.
    "h2>=4.1 ; sys_platform == 'linux'",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
